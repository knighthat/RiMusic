package app.kreate.utils.priomap


/**
 * Represents the severity or importance level of a map entry.
 * The declaration order defines the natural sorting order ([LOW] -> [NORMAL] -> [HIGH]).
 */
enum class Priority { LOW, NORMAL, HIGH }